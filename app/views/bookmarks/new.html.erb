<%= simple_form_for [@list, @movie, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.association :list %>
  <%= f.association :movie %>
  <%= f.submit "Submit", class: "btn btn-primary" %>
<% end %>
